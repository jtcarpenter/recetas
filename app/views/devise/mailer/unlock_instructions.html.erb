<p><%= t("devise.mailer.unlock_instructions.hello") %> <%= @resource.email %>!</p>

<p><%= t("devise.mailer.unlock_instructions.account_is_account") %></p>

<p><%= t("devise.mailer.unlock_instructions.click_to_unlock") %></p>

<p><%= link_to t("devise.mailer.unlock_instructions.unlock_link"), unlock_url(@resource, :unlock_token => @resource.unlock_token) %></p>
