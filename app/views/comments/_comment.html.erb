<% if comment.user %>
<p>
  <b>Commenter:</b>
  <%= comment.user.name %>
</p>
<% end %>

<p>
  <b>Comment:</b>
  <%= comment.body %>
</p>

<% if user_signed_in? %>
  <p>
    <%= link_to 'Destroy Comment', [comment.post, comment], :confirm => 'Are you sure?', :method => :delete %>
  </p>
<% end %>